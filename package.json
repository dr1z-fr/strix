{
  "name": "strix-command",
  "private": true,
  "version": "1.0.0",
  "description": "STRIX Command Terminal — panel paramilitaire (Vercel + Supabase)",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel deploy --prod"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "pg": "^8.11.3"
  }
}
